# Results Directory

Model-specific outputs are written under `results/<model_slug>/`.

The utility pipeline writes raw A/B logprob comparisons, fitted Thurstonian
utility weights, treatment summaries, and robustness checks. The downstream
pipeline writes raw generations, extracted code, local BigCodeBench pass/fail
results, and paired base-vs-praise summaries.

